<article class="post">
  <div class="title">
    <a href="#" target="_blank">{{post.dataValues.title}}</a>
    <span>({{post.dataValues.post}})</span>
  </div>
  {{!-- <div class="meta">
    {{vote_count}}
    {{format_plural "point" vote_count}}
    by
    {{user.username}}
    on
    {{format_date created_at}}
    |
    <a href="/post/{{id}}">{{answers.length}}
      {{format_plural "answer" answers.length}}</a> --}}
  {{!-- </div> --}}
</article>